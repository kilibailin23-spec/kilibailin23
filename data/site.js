/* ==========================================================================
   站点信息 + 侧边栏导航

   最先要改的就是这个文件。SITE 是站名/简介/邮箱/背景图/粒子开关，
   NAV 是左侧边栏那几项。

   这个文件是从原来的单文件 index.html 里拆出来的，内容没动过。
   ========================================================================== */

/* ==========================================================================
   ① 站点信息 —— 先改这里
   ========================================================================== */
const SITE = {
  name: '白麟',                       // 显示名（页签、页脚、关于页用）
  mark: ' ',                         // 左上角方块里的字
  /* 顶栏左上角那个方块想放图片（比如一个小头像）就写路径，写 './logo.png' 这样。
     填了它 mark 那个字就不显示了。参考站就是这么干的 —— 那个蓝帽子小人
     就是一张图。建议用正方形，方块本身是 42×42，四个角会被裁圆。 */
  markImg: null,
  tagline: '写代码、记笔记、放作品。',
  intro: '这里是我的技术博客。平时折腾前端和一些奇怪的小工具，' +
         '顺手把过程和踩过的坑记下来。作品和可以下载的文件都在下面。',
  email: 'you@example.com',                // ← 换成你的
  github: 'https://github.com/yourname',   // ← 换成你的，不要就写 null

  /* ---- 首页最上面那行大字（横幅里的「艺术字」）----
     参考站那儿写的是一行花体英文（Blog Of Windchan），这儿就是同一个位置。
     banner 是文字本身 —— 想用英文、繁体、日语、符号都行，写什么显示什么。
     bannerFont 是字体，现在是微软雅黑。想玩花体就换成系统里装了的字体，比如
       '"Old English Text MT",serif'                   → 哥特体（Office 装过才有）
       '"华文行楷","STXingkai",cursive'                → 中文行楷
       "Segoe Print"   手写儿童体
       "Segoe Script"  手写行楷体
     想连字号一起调就写 bannerSize，比如 '4.2rem'；不写（null）用默认的自适应字号。
     注意字体得是**浏览器的电脑上装了**才有效，网页字体（@font-face）这套是
     零依赖、不联网加载的，所以别指望某个网上下的字体在这里能显示。 */
  banner: 'Blog Of KILI·BaiLin',                // ← 换成你想要的那行字
  bannerFont: '"Segoe Print",ui-sans-serif,system-ui,sans-serif',
  bannerSize: '3.8rem',                        // 例如 '4.2rem'；null = 用默认值

  /* ---- 首页抬头那几块（首页布局照参考站来：横幅 → 关于我 → 联系方式）----
     avatar 是你的头像图片路径，放进这个文件夹再写 './avatar.jpg' 这样；
     没填（或者图片加载不出来）就退回头像圈里显示 mark 那个字，也就是「白」。
     正方形图片效果最好 —— 圆圈会把四个角裁掉。
     avatarName 是头像**下面**那行小字，参考站那儿写着「没有未来的小风酱」，
     你可以拿来写昵称。不想要就写 null。
     signature 是右边那列的手写签名，hashtags 是中间的自我介绍，一行一条。 */
  avatar: null,                            // ← 换成 './avatar.jpg' 之类
  /* 头像的**背面**。头像圈现在是个硬币：点一下绕竖轴翻 180°，
     正面是 avatar，背面就是这张。不填的话背面退回用 avatar（翻过去还是同一张，
     看着像卡住了），所以想玩这个效果，两张图都得填。 */
  avatarBack: null,                        // ← 换成 './avatar-2.jpg' 之类
  avatarName: 'KiLi白麟',                      // ← 头像下面那行小字，不要就写 null
  greeting: '嗨，很高兴见到你 👋',
  hashtags: [
    '前端开发 / 偶尔写点后端',
    '喜欢折腾小工具，也喜欢把坑记下来',
    '在这里放文章、作品和能下载的文件'
  ],
  signature: '宁鸣而死，不默而生。',
  signatureTo: '— 白麟',                   // 签名的落款，不要就写 null

  /* ---- 图标 ----
     图标源文件都放在 ./icons/ 里，一个图标一个文件，文件名就是下面
     NAV / CATEGORIES 里 icon 字段填的那个名字（写 home 就是 icons/home.png）。

     ⚠️ 但页面并不直接读这些文件 —— 它们会被打包进 data/icons.js 变成内联数据，
        因为 mask 在 file:// 下会被浏览器当跨源资源拦掉，直接引用的话
        双击打开时图标全都看不见。所以：
          · 换了 / 加了图标之后，双击「更新图标.bat」重新打包一次；
          · iconDir 只是重新打包时的来源路径，以及没打包进去的图标的兜底。
        （详见 tools/build-icons.js 的开头，那里把原因写全了。）

     iconExt 决定用哪一套 —— 两套都已经打包在 data/icons.js 里，改这个值立刻生效，
     不用重新打包：
       '.png' → Icons8 Fluent（Win11 风格，圆润、笔画偏粗）
       '.svg' → 手绘线条版（更细更简，和上面那套混着看风格会不统一）

     图标是用 mask 渲染的：只取形状，颜色由 currentColor 给（见 .ico-img 的样式）。*/
  iconDir: './icons/',
  iconExt: '.png',

  /* ---- 背景图：白天一张、夜里一张 ----
     换成自己的照片：把两张自己的图片放进这个文件夹，然后改下面两行，
     比如 bgDay: './bg-day.jpg'、bgNight: './bg-night.jpg'。
     两张都会铺满整个视口（cover、居中），并在切换深浅色时交叉淡入 ——
     所以它跟具体哪个页面无关，是**整个网站**的背景。
     只想用一张图：两行写成同一个路径即可，夜里那张就自动不生效。
     完全不想要背景图：两张都写 null。 */
  bgDay:   './bg.svg',
  bgNight: './bg-night.svg',
  bgVeil: 0.62,          // 背景图上的遮罩浓度，0~1。数字越大，文字越清楚，
                         // 背景图越淡。觉得字看不清就调大，觉得图太糊就调小。

  /* ---- 背景粒子 ----
     飘在背景上的那层小几何图形（在背景图和遮罩之上、正文之下）。
     亮色主题用彩色的小三角/碎片/菱形/六边形/方块，颜色取自 --reflection-*；
     暗色主题自动换成会呼吸的白色小圆点和星星。 */
  particles: {
    enabled: true,       // 不想要这层特效就写 false
    count: 80,           // 粒子个数。嫌闹腾就调到 30 左右
    speed: 0.5           // 漂浮速度，1 大约是它的两倍快
  },

  /* ---- 暗色 / 夜间模式 ---- */
  allowDark: true,       // 改成 false 就是彻底关掉暗色：锁死亮色，侧边栏那个
                         // 主题按钮也会藏起来。想只保留亮色版就写 false。
  nightFrom: 18,         // 「跟随时间」模式下，18 点之后自动切暗色
  nightTo: 6,            // 早上 6 点之后自动切回亮色
                         // 注意是跨午夜的区间（18 → 次日 6），写反了也能用，
                         // 代码会自己判断该按哪种区间算。
  /* ---- 开发者模式 ----
     入口还是顶栏左上角那个方块：长按 3 秒（会抖、有个进度环，然后炸开），
     炸完露出一个齿轮按钮，**点一下就是开关** ——
       开：页面上直接长出各种编辑控件（写文章按钮、卡片上的编辑/删除、
           图片点一下就换……），所见即所得，不再是弹窗里填表单；
       关：再点一下齿轮，控件全收起来。

     右键点方块 = 恢复成普通方块（把齿轮藏回去），这条一直有效，
     不用进开发者模式就能撤销，免得第一次玩的人点开了退不回去。

     ⚠️ 这里**没有密码**了。原来那对账号密码是明文写在这个文件里的，
        而文件是要一起部署上去的 —— 任何人按 F12 就能看到。
        静态站（双击 index.html 就能开，没有服务器）根本不存在
        「验证」这个环节，挡不住的。所以别把要紧的东西放进来：
        开发者模式能改的，全都只存在**你自己这台电脑的浏览器里**
        （localStorage），改的是「你眼前显示成什么样」，不是磁盘上的文件，
        别人访问你的站看到的还是 data/*.js 里原来那份。

     写 dev: false 就彻底关掉长按彩蛋，方块退化成普通的「回首页」按钮。
     （想从已经被点开的状态退回去：右键点方块。） */
  dev: true,

  footer: '© ' + new Date().getFullYear() + ' 白麟 · 用 HTML 手写着玩'
}

/* ==========================================================================
   导航
   ========================================================================== */
const NAV = [
  { hash: '#/',           label: '首页', icon: 'home' },
  { hash: '#/posts',      label: '文章', icon: 'posts' },
  { hash: '#/sites',      label: '网站导航', icon: 'compass' },
  { hash: '#/works',      label: '作品', icon: 'works' },
  { hash: '#/downloads',  label: '下载', icon: 'downloads' },
  { hash: '#/about',      label: '关于', icon: 'about' }
]

